# Dashboard Spec — {{project_name}}

> Generated by `dashboard-designer` on {{date}}.
> This document is the source of truth for the dashboard's GUI. The HTML
> scaffold is regeneratable from this spec; if they disagree, the spec wins.

---

## 1. Purpose

**One-line summary:**
_{{one-sentence description of what this dashboard is for}}_

**Primary user:**
_{{who uses it — individual researcher / team / customer demo}}_

**Success criterion:**
_{{what "done" looks like — e.g. "I can visually compare two training runs
without leaving the browser"}}_

---

## 2. Axis Decisions

| Axis | Decision |
|---|---|
| **1. Data Source** | _{{static files / tailing log / live stream / API}}_ |
| **   Embedded tools** | _{{none / TensorBoard / MLflow / Grafana / custom}}_ |
| **2. Layout Archetype** | _{{sidebar+main / split-pane+inspector / grid-of-cards / timeline / single-canvas}}_ |
| **3. Interaction Model** | _{{pick from: selection, brushing, playback, filter, comparison, read-only}}_ |
| **4. Visualization Widgets** | _{{summary list — details in §4}}_ |
| **5. Tech Stack** | _{{single HTML / React / React+FastAPI / Streamlit}}_ |
| **   Deployment** | _{{local file / static hosting / Dockerized server}}_ |
| **6. Style** | _{{theme / density / branding / live indicators}}_ |

---

## 3. Layout Sketch

Plain-text ASCII sketch of the layout. Label each region.

```
{{ASCII sketch here}}
```

**Region responsibilities:**
- _Region A:_ {{what goes here}}
- _Region B:_ {{what goes here}}
- …

---

## 4. Widgets & Data Bindings

For each widget, record: name, archetype region, data shape, data source, any
interactions it emits or listens to.

### Widget W1 — {{widget name, e.g. "Training loss line chart"}}

- **Region:** _{{e.g. Main content area, Tab "Metrics"}}_
- **Library:** _{{e.g. Chart.js line chart}}_
- **Data shape:**
  ```
  [{ step: number, loss: number, accuracy: number }, ...]
  ```
- **Data source:** _{{file path, API endpoint, or stream}}_
- **Refresh policy:** _{{load-once / poll every N sec / push}}_
- **Interactions:**
  - Emits: _{{e.g. "brushed range → filters log stream"}}_
  - Listens to: _{{e.g. "run selection in sidebar → switches dataset"}}_

_(repeat for W2, W3, … — one subsection per widget)_

---

## 5. Interaction Flows

Write 2–4 short scenarios describing how a user accomplishes their main
tasks. Each flow names the widgets touched and the state changes.

**Flow 1 — {{e.g. "Compare run A and run B on validation accuracy"}}:**
1. _User clicks run A in sidebar (W0) → main chart (W1) loads A's data._
2. _User ctrl-clicks run B → chart overlays B's curve with a second color._
3. _User hovers a diverged step → tooltip shows step number and both values._

_(repeat for other flows — focus on the most common 2–3 tasks, not edge cases)_

---

## 6. Data Contracts

Formal list of the inputs the dashboard consumes. This is what the backend
(or data pipeline) must produce.

| Input | Format | Source | Example |
|---|---|---|---|
| _{{metrics.json}}_ | _{{JSON array}}_ | _{{/path/to/run/metrics.json}}_ | _{{[{step:1, loss:2.3}, ...]}}_ |
| _{{config.yaml}}_ | _{{YAML dict}}_ | _{{/path/to/run/config.yaml}}_ | _{{...}}_ |
| … | | | |

---

## 7. Out-of-Scope / Deferred

Things considered but intentionally not in this spec:
- _{{e.g. "Auth — running locally, single user"}}_
- _{{e.g. "Mobile layout — desktop only"}}_
- _{{e.g. "Accessibility audit — follow-up pass"}}_

---

## 8. Implementation TODOs

Concrete checklist for whoever (Cursor / uf-implementor / hkjoo himself)
implements this spec:

- [ ] Replace sample data in scaffold with real data source (§6)
- [ ] Wire up selection/playback state to widgets (§5)
- [ ] Style polish — {{specific items if any}}
- [ ] Deploy — {{how to serve}}

---

## 9. Changelog

| Date | Change | Author |
|---|---|---|
| {{date}} | Initial spec from dashboard-designer interview | {{user}} |

# PROJECT_RULES.md — <프로젝트명> (프로젝트 바인딩)

> GLOBAL_RULES.md Rule 10이 요구하는 프로젝트 귀속 파일. **프로젝트 특정 사실은 전부 여기에 있고, GLOBAL_RULES.md에는 없다.**
> 우선순위: 프로젝트 사실은 본 파일 > 과거 리뷰독. 프로세스 규율(로그·ID·검증)은 GLOBAL_RULES가 우선.
> [템플릿 사용법: <> 자리를 채우고, 해당 없는 절은 "해당 없음"으로 남긴다 — 삭제하지 않는다. 빈 절은 "아직 결정 안 됨"의 기록이다.]

## 1. 프로젝트 정체

- 대상: <시스템/제품 한 줄 정의>
- 구조: <핵심 구성요소와 데이터 흐름 한 단락>
- 범위 외: <이 프로젝트가 책임지지 않는 것 — 명시하지 않으면 감사가 범위 밖을 지적한다>
- 언어/빌드: <언어, 빌드 시스템, 정본 빌드 트리, 시험 러너>

## 2. 정본 문서 (canonical documents)

| 역할 | 파일 | 비고 |
|---|---|---|
| 설계 정본 | <경로> | 현재 상태 서술이 원칙(이력은 review_docs로) |
| 발견·결정 대장 | `graph/edges.csv` | F-/D-/M- 상태의 단일 정본 (GLOBAL Rule 11) |
| 기계 검사 설정 | `graph/checks_config.json` | 본 프로젝트 귀속 질의 (GLOBAL Rule 14) |

존재하지 않는 문서를 정본으로 인용하지 않는다.

## 3. 원년 (ledger epoch)

- **원년: <YYYY-MM-DD>** (프로젝트 시작일 = 원년이면 소급 문제가 없다 — 이 템플릿의 존재 이유)
- P-ID는 P-001부터. 발견·결정은 발생 즉시 대장에 등록한다.

## 4. 도메인 불변식 (graph_checks가 검사)

| ID | 불변식 | 현재 상태 |
|---|---|---|
| INV-1 | <예: 선언된 config 상수는 코드에서 참조된다> | |
| INV-2 | <예: enum 값 ⊆ 계약 선언 범위> | |

`graph/checks_config.json`에 기계 검사로 등록하고, 검사 불가능한 불변식은 그 사유를 여기 적는다.

## 5. 유효 결정 대장 요약 (정본은 graph/edges.csv)

| ID | 결정 | 일자 |
|---|---|---|
| D-001 | <첫 결정> | |

## 6. 형식 오버라이드 (GLOBAL Rule 5에 대한)

- <해당 없으면 "없음 — GLOBAL 기본값 사용">

## 7. 증거 규약

- 시험 실행 증거: <경로 규칙, 예: logs/<run_id>/> — 문서는 수치 대신 경로를 인용 (GLOBAL Rule 13)
- 코드 인용은 `file:line` + 원문.

## 8. 알려진 미해결 전제

- <열람 불가 원본, 외부 의존 등 — U-항목. 없으면 "없음">
